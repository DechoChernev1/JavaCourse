package org.rewedigital;

public class Enums {
    public enum AccountType {
        REGULAR, VIP
    }
    public enum WithdrawType {
        ATM, BANK_DESK
    }
    public enum PaymentType {
        ONLINE, ONLINE_FAST, POS
    }
}
